import Head from "next/head";
import Image from "next/image";

export default function Home() {
  return (
    <div>
      <Head>
        <title>Candles</title>
        <meta name="description" content="Candles Blockchain" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
    </div>
  );
}
